pub struct Book {
    // Define the fields of the struct
    pub title: String,
    pub author: String,
    pub year: i32,
}

impl Book {
    pub fn new(title: &str, author: &str, year: i32) -> Self {
        // Your code here...
        unimplemented!()
    }
}
